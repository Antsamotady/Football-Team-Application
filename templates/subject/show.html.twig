{% extends 'dashboard-base.html.twig' %}

{% block title %}Matière{% endblock %}

{% block body %}
    <h1>Détaille de la matière</h1>

    <div class="card-p pt-5 bg-body flex-grow-1 mb-4">
        <!--begin::Row-->
        <div class="row g-0">
            <!--begin::Col-->
            <div class="col mr-8">
                <!--begin::Label-->
                <div class="fs-7 text-muted fw-bold">Nom de la matière</div>
                <!--end::Label-->
                <!--begin::Stat-->
                <div class="d-flex align-items-center">
                    <div class="fs-4 fw-bolder">{{ subject.name }}</div>
                </div>
                <!--end::Stat-->
            </div>
            <!--end::Col-->
            <!--begin::Col-->
            <div class="col">
                <!--begin::Label-->
                <div class="fs-7 text-muted fw-bold">Coefficient</div>
                <!--end::Label-->
                <!--begin::Stat-->
                <div class="fs-4 fw-bolder">{{ subject.coefficient }}</div>
                <!--end::Stat-->
            </div>
            <!--end::Col-->
        </div>
        <!--end::Row-->
        <!--begin::Row-->
        <div class="row g-0 mt-8">
            {# <!--begin::Col-->
            <div class="col mr-8">
                <!--begin::Label-->
                <div class="fs-7 text-muted fw-bold">ID</div>
                <!--end::Label-->
                <!--begin::Stat-->
                <div class="fs-4 fw-bolder">{{ subject.id }}</div>
                <!--end::Stat-->
            </div>
            <!--end::Col--> #}
            <!--begin::Col-->
            <div class="col">
                <!--begin::Label-->
                <div class="fs-7 text-muted fw-bold">Enseignant</div>
                <!--end::Label-->
                <!--begin::Stat-->
                <div class="d-flex align-items-center">
                    <div class="fs-4 fw-bolder">{{ subject.teacher }}</div>
                </div>
                <!--end::Stat-->
            </div>
            <!--end::Col-->
        </div>
        <!--end::Row-->
    </div>

    <div class="d-flex justify-content-between">
        <a href="{{ path('subject_index') }}" class="btn btn-secondary w-66">Voir toutes les matières</a>
        <div class="d-flex justify-content-end">
            <a href="{{ path('subject_edit', {'id': subject.id}) }}" class="btn btn-primary w-66 me-2">Editer</a>
            {{ include('subject/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
