<!doctype html>
<html lang="en">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width">
    <title>Welcome to the Space Bar!</title>
    <style>
        body {
            margin: 5px;
            padding: 5px;
            font-family: Helvetica, Arial, sans-serif;
        }
        h1 {
            /* top | right | bottom | left */
            padding: 5px 0 5px 5px;
            font-weight: normal;
        }
        hr {
            border: none;
            border-top: 3px solid #264459;
            margin: 20px;
        }
        img {
            background-color: #f3f3f3;
        }
    </style>
</head>
<body>
<div class="body">
    <div class="container">
        <div class="content">
            <h1 class="text-center">Informations sur l'utilisateur : </h1>
            <ul class="block">
                <li>
                    Horodatage : <strong>{{ horodatage }}</strong>
                </li>
                <li>
                    Nom : <strong>{{ login }}</strong>
                </li>
                <li>
                    IP : <strong>{{ IP }}</strong>
                </li>
            </ul>
        </div>

    </div>
    <div>
        <p>&nbsp;</p>
        <a href="{{ url('list_users') }}">
            <img src="{{ absolute_url(asset('assets/media/logos/logo_pi-planner.png')) }}" class="logo" alt="Voir">
        </a>
    </div>
</div>
</body>
</html>
