{% extends 'dashboard-base.html.twig' %}

{% block title %}Modification{% endblock %}

{% block body %}
    {{ include('location/_form.html.twig', {'button_label': 'Update', 'is_edit': true}) }}
    <hr>
    <div class="d-flex justify-content-between">
        <a href="{{ path('location_index') }}" class="btn btn-secondary w-66">Liste des centres</a>
        {{ include('location/_delete_form.html.twig') }}
    </div>
{% endblock %}
