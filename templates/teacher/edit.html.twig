{% extends 'dashboard-base.html.twig' %}

{% block title %}Modification{% endblock %}

{% block body %}
    {{ include('teacher/_form.html.twig', {'button_label': 'Update', 'is_edit': true}) }}
    <hr>
    <div class="d-flex justify-content-between">
        <a href="{{ path('teacher_index') }}" class="btn btn-secondary w-66">Liste des enseignants</a>
        {{ include('teacher/_delete_form.html.twig') }}
    </div>
{% endblock %}
