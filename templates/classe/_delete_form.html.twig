<form method="post" action="{{ path('classe_delete', {'id': classe.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cette entrée?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe.id) }}">
    <button class="btn btn-warning">Delete</button>
</form>
