{% extends 'dashboard-base.html.twig' %}

{% block title %}Fiche de la {{ classe.name }} {% endblock %}

{% block body %}

    {% for notice in app.flashes('success') %}
        <div class="alert alert-success" role="alert">{{ notice }}</div>
    {% endfor %}
    {% for notice in app.flashes('error') %}
        <div class="alert alert-danger" role="alert">{{ notice }}</div>
    {% endfor %}

    <h1>Renseignement sur la classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ classe.name }}</td>
            </tr>
            <tr>
                <th>Nb étudiants</th>
                <td>{{ classe.students|length }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>...</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex justify-content-between">
        <a href="{{ path('classe_index') }}" class="btn btn-secondary w-66">Voir toutes les classes</a>
        <div class="d-flex justify-content-end">
            <a href="{{ path('classe_edit', {'id': classe.id}) }}" class="btn btn-primary w-66">Editer</a>
            {{ include('classe/_delete_form.html.twig') }}
        </div>
    </div>

    <hr>

	<!--begin::Card-->
	<div class="card">
    {{ include('student/_student_list_table_body.html.twig') }}
    </div>
    <!--end::Card-->
{% endblock %}

{% block foot_script %}
	{{parent()}}
	<script type="text/javascript">
		$("#kt_subscriptions_table").DataTable();
	</script>
{% endblock %}
